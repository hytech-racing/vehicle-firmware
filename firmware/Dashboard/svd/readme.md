This is the svd file for the memory map descriptions of this chip. Used for debugging, it will show the names and memory locations of peripheral registers and may also decode some entries.

This SVD file was harvested from the STM32CubeProgrammer installation on MacOS with the filepath: STM32CubeProgrammer.app/Contents/MacOs/SVD